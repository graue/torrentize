Unimplemented:
 * ignoring files matching certain wildcards

Untested:
 * -p, mark torrent as private
 * -q, quiet mode
 * -R, rename file or topdir
 * -b, custom piece size

Limitations:
 * doesn't work on big endian systems yet
